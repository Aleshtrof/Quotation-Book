Get the application URL by running these commands:
kubectl port-forward service/{{ include "qo.fullname" . }}-frontend {{ .Values.frontend.service.hostPort }}:{{ .Values.frontend.service.containerPort }} -n {{ .Release.Namespace }}
And open the URL: https://localhost:{{ .Values.frontend.service.hostPort }}/
